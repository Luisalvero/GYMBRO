# GymBuddy Environment Configuration
# This file is for documentation only - actual config is in src/helpers.php

# Database Configuration (DDEV defaults)
DB_HOST=db
DB_NAME=gymbro
DB_USER=db
DB_PASS=db
DB_ROOT_PASS=root

# Application
APP_NAME=GymBuddy
APP_URL=https://gymbro.ddev.site

# Session Configuration
SESSION_LIFETIME=7200
SESSION_NAME=gymbro_session

# Security
# Sessions are configured in src/helpers.php with:
# - HttpOnly cookies
# - Strict mode
# - SameSite=Lax
# - CSRF protection on all POST requests

# Password Requirements
# - Minimum 8 characters
# - At least 1 uppercase letter
# - At least 2 digits
# - At least 2 symbols from @#!?
# Regex: /^(?=.*[A-Z])(?=(?:.*\d){2,})(?=(?:.*[@#!?]){2,}).{8,}$/
